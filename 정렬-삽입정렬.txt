#현재 리스트가 거의 정렬된 상태라면 매우 빠르게 동작
#O(N^2)
arr = [1, 7, 3, 4]

for i in range(1, len(arr)):
  for j in range(i, 0, -1): #i부터 1까지 1씩 감소하며 반복
    if arr[j] < arr[j-1]:
      arr[j], arr[j-1] = arr[j-1], arr[j]
    else: #자기보다 작은 데이터를 만나면 그 위치에서 멈춤
      break
print(arr)

